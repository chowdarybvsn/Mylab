pipeline {
    agent any 
    stages{
         stage ('checkout') {
              step {
                git branch:"master", url = "https://github.com/chowdarybvsn/Mylab.git"
              }
         }
    }  
}